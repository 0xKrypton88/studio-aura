# Agent instructions

Read `REFERENCE.md` before changing the site.

This is feature-heavy UI work. Use strict vertical RED→GREEN for substantive behavior, then run the production build and browser acceptance checks. Preserve the original files under `reference/` byte-for-byte. One implementation owner; no review loops. Do not deploy, change DNS, mutate MCC, or commit/push unless the orchestrator explicitly authorizes it.
